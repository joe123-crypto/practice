import {Inter,Lusitana,Nunito} from "next/font/google";

export const inter = Inter({subsets: ['vietnamese']}); 
export const lusitana = Lusitana({weight: "700", subsets: ['latin']});
export const nunito = Nunito({weight:"800",style:'italic',subsets:['latin']});
